<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary1</item>
        <item name="toolbar">@color/colorPrimary1</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark1</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundcolor">@color/background</item>
        <item name="background_2">@color/background_2</item>
        <item name="textcolor">@color/textcolor</item>
        <item name="button_txt">@color/button_txt</item>
        <item name="button_color">@color/colorPrimary1</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- Night / dark Mode theme. -->
    <style name="darktheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryDT</item>
        <item name="toolbar">@color/colorPrimaryDT</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkDT</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundcolor">@color/backgroundDT</item>
        <item name="background_2">@color/background_2DT</item>
        <item name="textcolor">@color/textcolorDT</item>
        <item name="button_txt">@color/button_txt</item>
        <item name="button_color">@color/button_color</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

</resources>
